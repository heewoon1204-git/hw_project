# 배포 자동화 구축

## Argo CD 

Argo CD를 이용해 배포하면 GitHub Actions가 EKS API에 접근하지 않아도됨. <br>
Argo CD가 EKS 내부에서 Kubernetes API와 통신하기 때문.

```bash
                GitHub
                  │
                git push
                  │
                  ▼
        ┌────────────────────┐
        │   GitHub Actions   │
        │                    │
        │  ① Test            │
        │  ② Docker Build    │
        │  ③ ECR Push        │
        └─────────┬──────────┘
                  │
                  ▼
                 ECR
                  │
                  │
        Git Manifest 변경
                  │
                  ▼
        ┌────────────────────┐
        │      Argo CD       │
        │       in EKS       │
        └─────────┬──────────┘
                  │
                  ▼
                 EKS
                  │
          cloud-portfolio
```

## Argo CD 적용하기

### 1. 기존 GitHub Actions를 CI-only로 바꾸기

이전에는 기존 GitHub Actions으로 CD까지 하려고 했지만, 네트워크 접속 이슈로 Argo CD로 CD를 할것이기 때문에 기존 Actions를 변경해야함. <br>
GitHub Actions은 ECR push하고 deployment에서 이미지를 최신 이미지로 변경하는 것까지만 !

```bash
# 변경 후 
name: Build and Push Cloud Portfolio

on:
  push:
    branches:
      - main
    paths:
      - "cloud-portfolio/**"

permissions:
  contents: write
  id-token: write

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: cloud-portfolio
  MANIFEST_FILE: k8s/cloud-portfolio/deployment.yaml

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      # ==========================================
      # 1. Checkout
      # ==========================================

      - name: Checkout
        uses: actions/checkout@v6


      # ==========================================
      # 2. AWS OIDC Authentication
      # ==========================================

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          role-to-assume: arn:aws:iam::245067526307:role/github-actions-hw-project
          aws-region: ${{ env.AWS_REGION }}


      # ==========================================
      # 3. Verify AWS Identity
      # ==========================================

      - name: Verify AWS identity
        run: |
          aws sts get-caller-identity


      # ==========================================
      # 4. Login to ECR
      # ==========================================

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2.1.6


      # ==========================================
      # 5. Build and Push Docker Image
      # ==========================================

      - name: Build and push image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}

        run: |
          docker build \
            --platform linux/amd64 \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            ./cloud-portfolio

          docker push \
            $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG


      # ==========================================
      # 6. Update Kubernetes Manifest
      # ==========================================

      - name: Update image tag in manifest
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}

        run: |
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          manifest = Path(os.environ["MANIFEST_FILE"])

          image = (
              f'{os.environ["ECR_REGISTRY"]}/'
              f'{os.environ["ECR_REPOSITORY"]}:'
              f'{os.environ["IMAGE_TAG"]}'
          )

          text = manifest.read_text()

          pattern = r'(^\s*image:\s*).*$'
          updated, count = re.subn(
              pattern,
              rf'\1{image}',
              text,
              count=1,
              flags=re.MULTILINE
          )

          if count != 1:
              raise SystemExit(
                  "cloud-portfolio image entry not found"
              )

          manifest.write_text(updated)

          print(f"Updated image to: {image}")
          PY


      # ==========================================
      # 7. Commit and Push Manifest
      # ==========================================

      - name: Commit and push manifest
        env:
          IMAGE_TAG: ${{ github.sha }}

        run: |

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add "$MANIFEST_FILE"

          if git diff --cached --quiet; then
            echo "No manifest changes detected."
            exit 0
          fi

          git commit \
            -m "chore: update cloud-portfolio image to ${IMAGE_TAG}"

          git push
```
<br>

### 2. 변경한 내용 push 후 Actions 확인하기

파일 수정 후 git push했을 때 gitActions에 의해 ECR에 자동으로 이미지가 배포된 것을 확인함

<p align="left">
  <img src="./images/github-actions-push.png" width="700">
</p>

<br>

### 3. Kubernetes manifest를 Git에서 관리하는 구조로 정리하기

```bash
# 목표 구조
hw_project/
├── .github/
│   └── workflows/
│       └── deploy.yml          # CI: 테스트 + Docker build + ECR push
│
├── cloud-portfolio/
│   ├── app.py
│   ├── Dockerfile
│   ├── requirements.txt
│   └── templates/
│
└── k8s/
    └── cloud-portfolio/
        ├── deployment.yaml
        ├── service.yaml
        └── ingress.yaml

# 목표 역할 분리
cloud-portfolio/** 수정
        ↓
GitHub Actions
        ↓
   Docker → ECR


    k8s/** 수정
        ↓
     Argo CD
        ↓
       EKS
```

1. Argo CD가 바라볼 manifest 경로 폴더 생성하기

    ```bash
    hw_project/
    ├── .github/
    │   └── workflows/
    ├── cloud-portfolio/
    └── k8s/
        └── cloud-portfolio/
    ```

2. deployment.yaml 파일 생성하기 <br>
    "kubectl get deployment -o yaml"로 현재 yaml 파일보면 내용이 많지만, 현재 클러스터가 관리하는 값들이 추가적으로 나오는거라 git에 전부 저장할 필요가 없음.

    Git에는 "원하는 상태(desired state)"만 넣음!! 

    ```bash
    apiVersion: apps/v1
    kind: Deployment

    metadata:
    name: cloud-portfolio
    namespace: application

    spec:
    replicas: 2

    selector:
        matchLabels:
        app: cloud-portfolio

    template:
        metadata:
        labels:
            app: cloud-portfolio

        spec:
        containers:
            - name: cloud-portfolio

            image: 245067526307.dkr.ecr.ap-northeast-2.amazonaws.com/cloud-portfolio:cc900d7efd7ac0effeeb0aecedd2e76ee0192bd1

            ports:
                - containerPort: 5001
                protocol: TCP

            envFrom:
                - secretRef:
                    name: cloud-portfolio-db

            resources:
                requests:
                cpu: 100m
                memory: 128Mi

                limits:
                cpu: 500m
                memory: 512Mi
    ```

3. Service도 같이 생성하기

    ```bash
    apiVersion: v1
    kind: Service

    metadata:
    name: cloud-portfolio
    namespace: application

    spec:
    type: ClusterIP

    selector:
        app: cloud-portfolio

    ports:
        - name: http
        port: 80
        targetPort: 5001
        protocol: TCP
    ```

<br>

### 3. Argo CD를 EKS에 설치하기

1. Argo CD namespace 생성

    ```bash
    kubectl create namespace argocd
    ```

2. Argo CD 설치하기

    ```bash
    # 방법 1
    kubectl apply \
        -n argocd \
        --server-side \
        --force-conflicts \
        -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
    
    # 방법 2
    # helm으로 설치
    helm repo add argo https://argoproj.github.io/argo-helm
    helm repo update
    helm install argocd argo/argo-cd \
        -n argocd 
    ```

    - argocd-application-controller : 실제 Kubernetes 리소스 상태를 비교하고 Sync하는 핵심 컴포넌트
    - argocd-applicationset-controller : 여러 Application을 자동 생성/관리
    - argocd-repo-server : Git Repository에서 manifest를 가져오고 렌더링
    - argocd-server : Argo CD UI + API 서버
    - argocd-redis : Argo CD 내부 캐시/상태 저장
    - argocd-notifications-controller : Sync/배포 상태 등의 알림 처리
    - argocd-dex-server : 로그인/인증 관련 컴포넌트

<br>

### 4. Argo CD Application 만들기

1. Argo CD UI 접속하기

    먼저 포트포워딩으로 UI 접속하기
    ```bash
    # 포트포워딩
    kubectl port-forward svc/argocd-server -n argocd 8080:443

    # 접속
    https://localhost:8080

    # 초기 비밀번호 조회
    kubectl -n argocd get secret argocd-initial-admin-secret \
        -o jsonpath="{.data.password}" | base64 -d
        echo
    ```

2. Application 만들기

    NEW APP 클릭

    ```bash
    Application Name : cloud-portfolio
    Project : default
    Sync Policy : Manual
    Repository URL : https://github.com/heewoon1204-git/hw_project.git
    Revision : main
    Path : k8s/cloud-portfolio
    Cluster : https://kubernetes.default.svc 
      -> Argo CD 자체가 지금 EKS 안에서 돌아가고 있으니까 https://kubernetes.default.svc가 현재 EKS 클러스터를 의미 
    Namespace : application
    ```

3. Auto-Sync 키기 
    DETAILS -> SYNC POLICY -> ENABLE AUTO-SYNC