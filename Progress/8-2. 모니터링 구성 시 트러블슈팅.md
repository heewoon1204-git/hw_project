# 모니터링 구성하다가 발생한 장애

## 1. helm kube-prometheus-stack으로 모니터링 설치하다가 kube-prometheus-stack-prometheus-node-exporter pending 장애 발생

### 장애 현상

elm kube-prometheus-stack으로 모니터링 설치하는데 프로메테우스, 그라파나 등 다른 pod는 정상적으로 떴는데 kube-prometheus-stack-prometheus-node-exporter만 pending된 상태

```bash
Events:
  Type     Reason            Age                    From               Message
  ----     ------            ----                   ----               -------
  Warning  FailedScheduling  18m                    default-scheduler  0/4 nodes are available: 1 node(s) didn't have free ports for the requested pod ports, 3 node(s) didn't satisfy plugin(s) [NodeAffinity]. no new claims to deallocate, preemption: 0/4 nodes are available: 1 No preemption victims found for incoming pod, 3 Preemption is not helpful for scheduling.
```

### 장애 원인

node-exporter가 3개 세트가 있어서 이미 9100 포트를 사용하고 있기 때문에 port 문제로 pod가 pending 됐던 것

```bash
amazon-cloudwatch
└── node-exporter × 4        ← CloudWatch 쪽

prometheus-node-exporter
└── prometheus-node-exporter × 4  ← 별도로 설치한 것

monitoring
└── kube-prometheus-stack-prometheus-node-exporter × 4  ← 지금 설치한 것
```

### 장애 해결

kube-prometheus-stack 하나로 Prometheus 모니터링을 구성하고 기존 9100 포트 사용하고 있는 prometheus-node-exporter를 제거

1. DaemonSet 삭제

    DaemonSet 삭제하면 pod도 같이 삭제됨
    ```bash
    kubectl delete daemonset prometheus-node-exporter \
    -n prometheus-node-exporter
    ```


## 2. EBS CSI 설치 중 controller pod 생성 에러 발생

### 장애 현상

cli에서 ebs csi 설치 중 ebs-csi-node pod는 정상적으로 Running 됐으나 controller pod가 CrashLoopBackOff되는 장애 발생

```bash
# 설치 명령어
aws eks create-addon \
  --cluster-name hw-infra-eks-cluster \
  --addon-name aws-ebs-csi-driver \
  --region ap-northeast-2

# 에러 메세지
Message: FailedPrecondition desc = Failed health check (verify network connection and IAM credentials): dry-run EC2 API call failed: operation error EC2: DescribeAvailabilityZones, get identity: get credentials: failed to refresh cached credentials, no EC2 IMDS role found, operation error ec2imds: GetMetadata, request canceled, context deadline exceeded

E0813 03:05:59.492138    err="rpc error: code = FailedPrecondition desc = Failed health check (verify network connection and IAM credentials): dry-run EC2 API call failed: operation error EC2: DescribeAvailabilityZones, get identity: get credentials: failed to refresh cached credentials, no EC2 IMDS role found, operation error ec2imds: GetMetadata, canceled, context deadline exceeded" 

error waiting for CSI driver to be ready: CSI driver probe failed: rpc error: code = FailedPrecondition desc = Failed health check (verify network connection and IAM credentials): dry-run EC2 API call failed: operation error EC2: DescribeAvailabilityZones, get identity: get credentials: failed to refresh cached credentials, no EC2 IMDS role found, operation error ec2imds: GetMetadata, canceled, context deadline exceeded

Failed to create CSI client" err="failed probing CSI driver: CSI driver probe failed: rpc error: code = FailedPrecondition desc = Failed health check (verify network connection and IAM credentials): dry-run EC2 API call failed: operation error EC2: DescribeAvailabilityZones, get identity: get credentials: failed to refresh cached credentials, no EC2 IMDS role found, operation error ec2imds: GetMetadata, canceled, context deadline exceeded

```

### 장애 원인

"failed to refresh cached credentials, no EC2 IMDS role found" log를 보면 EBS Controller가 AWS EC2 API를 호출해야되는데, IAM 자격 증명이 전달되지 않고 있음.

즉, EBS CSI Controller가 IAM Role이 연결되어있지 않아서 발생한 문제 <br>

```bash
# ebs-csi-controller에 Pod Identity Association이 존재하는지 확인
aws eks list-pod-identity-associations \
  --cluster-name hw-infra-eks-cluster \
  --region ap-northeast-2 \
  --profile hw-infra

# 결과
# ebs-csi-controller에 설정되어 있지 않음
{
    "associations": [
        {
            "clusterName": "hw-infra-eks-cluster",
            "namespace": "kube-system",
            "serviceAccount": "aws-node",
            "associationArn": "arn:aws:eks:ap-northeast-2:245067526307:podidentityassociation/hw-infra-eks-cluster/a-jjd076rk5ei4sicom",
            "associationId": "a-jjd076rk5ei4sicom",
            "ownerArn": "arn:aws:eks:ap-northeast-2:245067526307:addon/hw-infra-eks-cluster/vpc-cni/2ccff514-015e-0844-1cb3-c86247672f68"
        },
        {
            "clusterName": "hw-infra-eks-cluster",
            "namespace": "external-dns",
            "serviceAccount": "external-dns",
            "associationArn": "arn:aws:eks:ap-northeast-2:245067526307:podidentityassociation/hw-infra-eks-cluster/a-m1wcd6sy3mu5d8jf3",
            "associationId": "a-m1wcd6sy3mu5d8jf3",
            "ownerArn": "arn:aws:eks:ap-northeast-2:245067526307:addon/hw-infra-eks-cluster/external-dns/c4cff514-0152-32f8-595a-2e4dc7df2882"
        },
        {
            "clusterName": "hw-infra-eks-cluster",
            "namespace": "amazon-cloudwatch",
            "serviceAccount": "cloudwatch-agent",
            "associationArn": "arn:aws:eks:ap-northeast-2:245067526307:podidentityassociation/hw-infra-eks-cluster/a-rmkwjrunpdjl3vqoh",
            "associationId": "a-rmkwjrunpdjl3vqoh",
            "ownerArn": "arn:aws:eks:ap-northeast-2:245067526307:addon/hw-infra-eks-cluster/amazon-cloudwatch-observability/88cff514-011c-10e1-38ac-95bea5312ef3"
        },
        {
            "clusterName": "hw-infra-eks-cluster",
            "namespace": "kube-system",
            "serviceAccount": "aws-load-balancer-controller",
            "associationArn": "arn:aws:eks:ap-northeast-2:245067526307:podidentityassociation/hw-infra-eks-cluster/a-xjysdpjc3mgmjpfk8",
            "associationId": "a-xjysdpjc3mgmjpfk8"
        }
    ]
}

```

### 장애 해결

EBS CSI Controller용 Pod Identity Association을 추가해주면 됨

1. Pod Identity Association 추가하기 위해서 먼저 IAM Role 생성

    ```bash
    Trusted entity type : AWS Service
    Use case : EKS - Pod Identity
    Permissions policies : AmazonEBSCSIDriverPolicy
    ```

2. Role을 ebs-csi-controller-sa에 Pod Identity로 연결

    EKS -> Access -> Pod Identity associations -> Create

    | 항목              | 값                         |
    | --------------- | ------------------------- |
    | IAM role        | 방금 만든 EBS CSI Driver Role |
    | Namespace       | `kube-system`             |
    | Service account | `ebs-csi-controller-sa`   |

3. 기존 에러났던 pod delete

    CrashLoopBackOff되r고 있는 ebs-csi-controller pod를 delete 해주면 pod가 새로 생기고, IAM Role을 연결했기 때문에 장애 해결