apiVersion: v1
kind: Service

metadata:
  name: cloud-portfolio
  namespace: application

spec:
  type: ClusterIP

  selector:
    app: cloud-portfolio

  ports:
    - name: http
      port: 80
      targetPort: 5001
      protocol: TCP